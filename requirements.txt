# Pinned to the versions the published results were produced with.
# See results/metrics/unified_results.json -> provenance for the exact
# torch/ultralytics build every number in the README came from.

# ── Core ──
numpy==2.0.2
opencv-python==4.13.0.88
pillow==11.3.0

# ── Data ──
pycocotools==2.0.10

# ── Classical baselines ──
scikit-image==0.24.0
scikit-learn==1.6.1

# ── Deep learning ──
# torch is intentionally unpinned here: the correct wheel depends on the
# platform (CUDA / ROCm / Apple MPS). Install it for your machine first, then
# this file. The run that produced the published numbers used torch 2.8.0 on MPS.
ultralytics==8.4.38

# ── Figures & notebooks ──
matplotlib==3.9.4
pandas==2.3.3
nbformat==5.10.4
nbconvert==7.17.1
ipykernel==6.31.1
